#ifndef BASIC_MATH
#define BASIC_MATH
 
// for a headerfile the containing forward declaration for an object from a local namespace still needs the namespace specified 
namespace basic_math {
int addTwo(int x, int y);
}

#endif